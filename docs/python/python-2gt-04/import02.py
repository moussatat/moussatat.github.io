# --- PYODIDE:code --- #
import math         # import librairie math

a = math.pi         # affecte à c la valeur de pi
b = math.sqrt(2)    # affecte à la racine carrée 
c = math.cos(1)     # affecte à b le cosinus de 1 radians

print(a , b , c)

import random       # import librairie random 

print( random.randint(1,6) )   # nombre aléatoire entre 1 et 6
